Use Ionic Framework UI components instead of custom HTML/CSS when building interfaces. Prefer ion- tags, use Ionic CSS variables for colors, use Ionicons for icons, and only fall back to custom styles when no Ionic equivalent exists.
