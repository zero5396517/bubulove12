import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bubulove12.app',
  appName: 'ionic-android-app',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
